
<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="<?php echo site_url()?>">Start<span class="green">BBS</span></a>
            <!--<a class=".btn .btn-default navbar-btn collapsed" data-target=".navbar-collapse" data-toggle="collapse"><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></a><a href="<?php echo site_url()?>" class="brand">Start<span class="green">BBS</span></a>-->
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li class="active"><a href="/" target=_blank>前台首页</a></li>               
            </ul>
            <ul class="nav navbar-nav navbar-right">
                @{
                    if (ViewBag.User != null)
                    {
                        <li><a href="@Url.Action("info","user",new{id= ViewBag.User.Uid})">@ViewBag.User.UserName</a></li>
                        <li><a href="@Url.Action("profile","settings")">个人设置</a></li>
                        //是否是管理员
                        if (true)
                        {
                            <li class=""><a href="@Url.Action("dashboard")">管理后台</a></li>
                        }
                        <li><a href="@Url.Action("loginout","user")" data-method="delete" rel="nofollow">退出</a></li>
                    }
                    else
                    {
                        <li><a href="@Url.Action("reg","user")">注册</a></li>
                        <li><a href="@Url.Action("login","user")">登入</a></li>
                    }
                }
            </ul>
        </div><!--/.nav-collapse -->

    </div>
</div>